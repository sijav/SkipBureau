# InfoPanel

## Props

- `heading`: The panel's own title in place of its register's name, as the guide's callouts have, Figma 179:1112. The kind still decides every colour, so a title cannot make one register look like another.
