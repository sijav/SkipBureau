# ContextChip

## Props

- `name`: Which piece of the reader's situation this is: nationality, city, status.
- `value`: Their answer. Without one the chip is unset: amber, dashed, and inviting.
- `prompt`: What an unset chip says instead of a value.
- `onClick`: Always editable, per the design, so a chip is always a button.
