# ShellProvider

## Props

- `place`: Null until the database has confirmed the address's country, and for an address without one.
- `ownsAskAtStart`: Whether the page at this address opens with its own Ask on screen, as Home does.
