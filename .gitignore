node_modules/
agent/todo.db-journal
agent/todo.db-wal
agent/todo.db-shm
dist/
build/
.env
.env.local
storybook-static/
playwright-report/
test-results/

# roast state: this machine, not the project
.claude/roast-result.md
.claude/roast-sessions.json
.claude/roast-last-message.tmp

.claude/todo.db-journal
.claude/todo.db-wal
.claude/todo.db-shm

__pycache__/

# the plan is working state for the current task

# compiled lingui catalogs, generated from the .po files
apps/web/src/locales/*.mjs

# the contract test's throwaway copy of the API source: it renames a field
# there rather than in the tree, so a killed run cannot leave one behind
apps/api/.contract/

# generated coverage reports
coverage/
